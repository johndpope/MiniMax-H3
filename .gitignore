*.safetensors

__pycache__/
*.pyc

# Phase 3 training output: logs, adapters and their metadata. The adapters are gitignored by
# the *.safetensors rule above; the logs are large, per-run and reproducible from the script.
scripts/scd/runs/
scripts/scd/*.log
